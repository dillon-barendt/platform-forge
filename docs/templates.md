# Template System

Templates are resolved through `TemplateRegistry`.

The first release supports bundled local templates. The registry is intentionally
small so GitHub-hosted templates can be added later without changing command or
generator boundaries.
