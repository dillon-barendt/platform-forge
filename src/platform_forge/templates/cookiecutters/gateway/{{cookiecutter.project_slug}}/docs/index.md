# {{ cookiecutter.project_name }}

This gateway scaffold provides the platform edge for the `{{ cookiecutter.domain }}`
domain.

## Boundaries

The gateway coordinates request routing, provider adapters, settings, and shared
edge concerns. Business logic belongs in downstream services.
