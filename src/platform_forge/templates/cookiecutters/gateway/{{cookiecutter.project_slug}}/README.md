# {{ cookiecutter.project_name }}

Generated by Platform Forge.

This repository is intentionally self-contained. Platform Forge is not required
after generation.

## Quickstart

```bash
make install
make dev
```

## Architecture

- `apps/gateway`: FastAPI edge gateway
- `apps/web`: optional browser frontend
- `docs`: project documentation
- Root `Makefile`: canonical orchestration commands

Configuration ownership:

- Root `.env`: shared local infrastructure only
- `apps/gateway/.env`: backend runtime configuration
- `apps/web/.env.local`: browser-safe frontend configuration

The root `Makefile` is the canonical local command interface. It orchestrates
tools, but runtime configuration is owned by each application.
